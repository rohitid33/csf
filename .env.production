# Production Environment Variables

# Database Configuration
# These should be set in your production environment and not committed to version control
MONGODB_URI=mongodb+srv://username:password@host/database?retryWrites=true&w=majority
DATABASE_URL=postgres://username:password@host:port/database

# Server Configuration
PORT=3000
NODE_ENV=production

# Authentication
# Generate secure random values for these in production
SESSION_SECRET=replace-with-secure-random-value-in-production
JWT_SECRET=replace-with-secure-random-value-in-production